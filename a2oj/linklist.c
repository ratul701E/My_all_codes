#include <stdio.h>
#include <stdlib.h>

typedef struct node {
	int number;
	struct node *next;
	struct node *prev;
}node;

int main () {
	
	//node *list = malloc(sizeof(node));
	//list->prev = NULL;
	//list -> next = NULL;
	//node *n = malloc(sizeof(node));
	//n->number = 1;
	//n->next = NULL;
	//n->prev = list;
	//list-> next = n;
	//n = malloc(sizeof(node));
	//n->number = 2;
	//n->next = NULL;
	//n->prev = list->next;
	//list->next->next = n;
	
	//while(list != NULL) {
		//node *n = list -> next;
		//printf("%i\n",list->number);
		//list = n;
	//}
	
	
	
}
